apiVersion: apps/v1
kind: Deployment
metadata:
  name: speech-stt
  namespace: speech
  labels:
    app: speech-stt
spec:
  replicas: 1
  strategy:
    type: Recreate   # single node GPU - a surge pod could never schedule anyway
  selector:
    matchLabels:
      app: speech-stt
  template:
    metadata:
      labels:
        app: speech-stt
    spec:
      runtimeClassName: nvidia
      nodeName: node3
      imagePullSecrets:
        - name: local-registry-cred
      containers:
        - name: speech-stt
          image: local-registry:5000/speech-stt:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token
                  key: HF_TOKEN
            - name: STT_MODEL_NAME
              value: "nvidia/parakeet-tdt-0.6b-v3"
            - name: STT_DEVICE
              value: "cuda"
            - name: HF_HOME
              value: "/models"   # cache and model location
            - name: TRANSFORMERS_CACHE
              value: "/models"
            - name: HF_HUB_OFFLINE
              value: "1"
            - name: TRANSFORMERS_OFFLINE
              value: "1"
          ports:
            - containerPort: 8001
          resources:
            requests:
              nvidia.com/gpu: 1
              cpu: "2"
              memory: 4Gi
            limits:
              nvidia.com/gpu: 1
              cpu: "4"
              memory: 8Gi
          volumeMounts:
            - name: models
              mountPath: /models
          startupProbe:
            httpGet:
              path: /healthz
              port: 8001
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8001
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8001
            periodSeconds: 20
            failureThreshold: 3
      volumes:
        - name: models
          hostPath:
            path: /mnt/local-fast/parakeet-model
            type: Directory